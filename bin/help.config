#!/usr/bin/env bash

set -eu
[ "${BASH_VERSINFO[0]}" -ge 3 ] && set -o pipefail

cat <<'HELP'
Configuration for asdf-svn
=========================

Additional Configure Options:
When building Subversion from source, several configuration options can be added.
The default configuration includes:

  --prefix="$prefix"
  --with-apr="$(command -v apr-1-config || command -v apr-config)"
  --with-apr-util="$(command -v apu-1-config || command -v apu-config)"
  --with-ssl
  --with-sqlite3
  --without-berkeley-db

Environment Variables:
The following environment variables can be used to customize the build:

ASDF_SVN_CONFIGURE_OPTIONS - Additional options to pass to the configure script
  Example: ASDF_SVN_CONFIGURE_OPTIONS="--enable-javahl --with-jdk=/path/to/jdk" asdf install svn 1.14.2

ASDF_SVN_CONCURRENCY - Number of parallel make jobs (default: auto-detected)
  Example: ASDF_SVN_CONCURRENCY=4 asdf install svn 1.14.2
HELP

exit 0
